# OpenCode Agent Rules

This directory contains rules and configuration for OpenCode agent.

## Memory Stack

See `.agents/rules/memory-stack.md` for the project memory stack contract.

## Workflows

See `.agents/workflows/` for memory-bank-update, graphify-refresh, wiki-ingest, wiki-query, and session-handoff workflows.
